import { createStore } from 'redux';
import reduce from './reducer';
import {add, subtract, multiply,divide, odd, even} from "./actions";

// TODO: Create a Redux Store

export const store = 

// Optional: Test your calculator by creating a listener that prints state updates to the console
// You can also test your actions and reducer here